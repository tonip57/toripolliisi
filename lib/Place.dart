class Place {
  String name;
  String category;
  String imgName;
  String infoText;
  List<bool> usersVote;
  List coordinates;
  int votes;

  Place(this.name, this.category, this.imgName, this.infoText, this.usersVote, this.coordinates, this.votes);
}